---
layout: layouts/base.njk
templateClass: tmpl-home
permalink: "{{ date | htmlDateSlug }}/{{ title | slugify }}/"
---

{# This is the template for individual blog posts–it builds the front matter properties into the page #}

<div class="wrapper post">
  <div>
    {# The post front matter includes the title property #}
    <h1 class='postTitle'>
      <span>{{title}}</span>
    </h1>
    <p>
      <time datetime="{{ date }}">{{ date | htmlDateString }}</time>
      <br/>
      {{ description }}
    </p>
    <hr/>

    {# Content from the body of the post markdown appears here #}
    {{ content | safe }}

    <hr/>
    <div class="controls">
    {# The previous and next post data is added in .eleventy.js #}
    {% if prevPost.url %}
      <p class="previous">
        <a href="{{ prevPost.url }}">{{ prevPost.data.title }}</a>
      </p>
    {% endif %}
    {% if nextPost.url %}
      <p class="next">
        <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a>
      </p>
    {% endif %}
    </div>
  </div>
</div>

<script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/11.3.1/highlight.min.js"></script>
<script>hljs.highlightAll();</script>
